# Public Vite variables may be exposed to browser code.
VITE_APP_NAME="Job Tracker Pro"

# Local-only AI insights use Ollama in the browser; no hosted API key is required.
VITE_OLLAMA_MODEL="qwen2.5:7b"

# Future private AI/API keys must stay server-side only and must not use VITE_.
# AI_PROVIDER_API_KEY=""
