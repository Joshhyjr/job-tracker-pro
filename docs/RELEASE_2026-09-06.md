# September 6 release preparation

This release combines the audited correctness fixes with the previously deployed Analytics/Follow-ups UI, preserving the September 2 production behavior. Separate commits keep that existing UI work distinct from the audit fixes and CI hardening.

## Validation

- Full suite against a temporary local Firestore emulator: **320 passed, zero skipped**.
- Browser, Node configuration, and API TypeScript checks: passed.
- ESLint: zero errors; seven existing Fast Refresh warnings in unchanged shared UI files.
- Production build and `git diff --check`: passed; existing bundle-size warnings remain.
- Previous-turn isolated browser verification covered Halifax midnight rollover, application creation, CSV/XLSX exports, canonical/custom column preservation, and exported activity-history linkage.
- CI actions were resolved from their official GitHub repositories and pinned to full commit SHAs. The Node application runtime remains 22 in CI; setup-node itself advances to v7.
- The dependency-review configuration failure was repaired by enabling GitHub dependency graph/alerts; the comparison endpoint returned successfully afterward.
- No Firestore rule deployment or production data migration is needed: the rules and stored schemas are unchanged.

## Cleanup decisions

| PR / branch group | Disposition | Evidence |
| --- | --- | --- |
| PRs #1 and #3 | Closed as superseded | Main already uses dependency-review-action v5 and CodeQL v4. |
| PRs #5, #6, #7, #8, #9 | Closed as superseded | Main's lockfile already resolves collapsible 1.1.14, tooltip 1.2.10, accordion 1.2.14, typescript-eslint 8.61.1, and menubar 1.1.18. |
| PRs #11 and #12 | Closed as duplicates | Their stable patch IDs exactly match retained PRs #13 and #14, respectively. |
| Seven remote branches already merged into main | Eligible for removal with expected-tip guards | `git merge-base` ancestry via `git for-each-ref --merged origin/main`; no unique commits are removed. |
| PR #16 | Superseded once this release merges | Release CI includes the requested setup-node v7 update with a verified SHA pin. |
| PR #19 | Superseded once this release merges | Release includes the local-calendar-date fix with unit and actual browser rollover verification. |
| Other unmerged feature/review branches; PRs #13 and #14 | Retained | They contain unique work outside this release; age alone is not a reason to delete them. |

No open GitHub issues were present. Local worktree pruning removes only metadata whose checkout location no longer exists; live worktrees are retained.

## Outstanding release checks

The local npm audit was blocked by the execution approval review because it sends dependency metadata to npm. Explicit approval is pending. The release PR's existing security workflow also runs npm audit; do not disable or bypass that check to publish the release.

The API limiter is still per-instance and live Vercel custom rules are empty. An edge/shared rate limiter remains a separate operational follow-up; no firewall rules or paid plan changes were applied during this release preparation.

Production initially points to deployment `dpl_4iuhw4k4BTQBAkXBoHyDbZUXNkju` on `www.jkivaria.com` and `jkivaria.com`. A successful release requires fresh CI results, a merge to main, a READY production deployment, and live browser verification; local commits alone do not establish those outcomes.
